// this is some pre-emptive commenting
// so is this

////////////////////
// this is a new one

package iDirect.DirectoryService;

//early comment 1
//early comment 2


enum Colors  { // my colours
    RED = 0; // hello
    GREEN = 1;
    // what does on here?
    BLUE = 2;
}

// mid comment1
// mid comment2

object Query { // a query
  option a = 0; // my my
  option b = 2001; // abc
}

object Query2 {
  option a = 0; 				// simple meta header option
  required uint32 b	   = 1;  		// simple require
  repeated string c = 7; 			// simple array
}

enum Numbers  {
    ONE = 1;
    TWO = 2;
}

//  repeated uint32 d = 6 [key = "string"];	// simple hash
