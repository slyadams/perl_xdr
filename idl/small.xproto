package test.a;

//
//a
//

enum X {
// a
//b
    DID_RECEIVED                              = 684 [ description = "DID Received" ];
// c
// d
}

enum Y {
	a = 1;
	b = 2;
}

object Query {
  required X				    = 1 [ default = 12 ];
  required string e			    = 2 [ default = "def 1" ];
  required string e			    = 3 [ default = 123 ];	
  repeated uint f			    = 4 [ key = unit32 ];	
}
